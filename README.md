# MaxHeap
This is my simple implementation of a Max-Heap from scratch. It features a class, MaxHeap, with simple functions typically used by Heaps. The file features a tester class which ensures the data structure works as intended.
